<div class="{{ $className }}">{{ $title }}</div>
<select
    class="tom-select-custom w-26"
    name="{{ $inputName }}"
    id="{{ $inputName }}">
    <option value="">{{ $selectOption }}</option>
</select>
